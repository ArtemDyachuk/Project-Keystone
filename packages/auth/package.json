{
  "name": "@keystone/auth",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "setup:firebase:dev": "./setup-firebase.sh --stage=dev --mode=update",
    "setup:firebase:prod": "./setup-firebase.sh --stage=prod --mode=update"
  },
  "dependencies": {
    "firebase": "^12.1.0",
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.2"
  }
}
[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmd = 'npm ci'

[phases.build]
cmd = 'npx nx build @keystone/cms-api'

[phases.start]
cmd = 'node apps/back-ends/cms-api/dist/main.js'

{
  "name": "@keystone/source",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "build": "turbo build",
    "build:safe": "npm test && turbo build",
    "dev": "turbo dev --filter=@keystone/cms --filter=@keystone/cms-api",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test --filter=@keystone/cms --filter=@keystone/cms-api",
    "test:security": "turbo test --filter='*security*'",
    "clean": "turbo clean && rm -rf node_modules",
    "setup:cognito": "cd packages/auth && npm run setup:cognito",
    "prepare": "husky"
  },
  "private": true,
  "packageManager": "npm@10.9.3",
  "dependencies": {
    "dotenv": "^17.2.1",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.5"
  },
  "devDependencies": {
    "@eslint/compat": "^1.1.1",
    "@eslint/eslintrc": "^3.3.1",
    "@eslint/js": "^9.8.0",
    "eslint": "^9.8.0",
    "eslint-config-prettier": "^10.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.29.0"
  },
  "workspaces": [
    "packages/*",
    "apps/front-ends/*",
    "apps/back-ends/*"
  ]
}

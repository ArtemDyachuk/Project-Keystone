# Railway Config as Code for monorepo

# Backend service (NestJS)
[[services]]
name = "cms-api"
root = "."
build = "npx nx run @keystone/cms-api:build:production"
start = "node apps/back-ends/cms-api/dist/main.js"

# Frontend service (Next.js)
[[services]]
name = "cms-web"
root = "."
build = "npx nx run @keystone/cms:build"
start = "npx nx run @keystone/cms:start -- -p $PORT"



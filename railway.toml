# Railway Config as Code
# This configuration is for the FRONTEND service (cms)
# Create a separate railway.toml for the backend service

[build]
builder = "nixpacks"
buildCommand = "npx nx build @keystone/cms"

[deploy]
startCommand = "cd apps/front-ends/cms && npx next start -p $PORT"


